# Copy this file to .env and fill in values before deploying.

# Required: password to access the app. Leave unset to disable auth (dev only).
AUTH_PASSWORD=selki123

# Optional: comma-separated list of allowed CORS origins.
# Defaults to "*" if unset (dev only).
# CORS_ORIGINS=https://selki.example.com