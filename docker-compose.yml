services:
  selki:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # Persist the SQLite database and uploaded audio files across restarts
      - selki-db:/app/data
      - selki-uploads:/app/uploads

volumes:
  selki-db:
  selki-uploads:
